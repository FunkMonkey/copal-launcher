---
- name: commands
  description: Shows a list of available commands

  components:
    - core
    - launcher

  connections:
    "launcher::query$": "core::commandQuery$"
    "core::commandNames$": "launcher::data$"
    "launcher::chosenListenItem$": "launcher::commandToExecute$"

- name: foobar
  description: Shows a list of available commands

  components:
    - core
    - launcher

  connections:
    "launcher::query$": "core::commandQuery$"
    "core::commandNames$": "launcher::data$"
    "launcher::chosenListenItem$": "launcher::commandToExecute$"
